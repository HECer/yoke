node_modules/
dist/
*.log
.yoke/worktrees/
.yoke/backup/

# Yoke's own retrofit output when dogfooding `yoke retrofit .` against this
# repo — these are generated artifacts, not source. Root-anchored so the canon's
# own files (canon/AGENTS.md, etc.) are unaffected.
/.claude/
/.codex/
/.gemini/
/.mcp.json
/AGENTS.md
/CLAUDE.md
/GEMINI.md
/RTK.md
/.yoke/config.yaml
/.yoke/prd.yaml

bench/.runs/
